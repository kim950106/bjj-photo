services:
  - type: web
    name: bjj-vision
    runtime: python
    rootDir: bjj-vision
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
